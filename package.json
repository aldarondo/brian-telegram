{
  "name": "brian-telegram",
  "version": "1.0.0",
  "description": "Telegram bot → Claude Code CLI → Telegram reply. Mobile access to Brian family skills.",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test tests/**/*.test.js"
  },
  "dependencies": {
    "express": "^4.21.0",
    "node-cron": "^3.0.3"
  },
  "engines": {
    "node": ">=20"
  }
}
